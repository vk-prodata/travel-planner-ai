import React from 'react';
// Remove the component entirely since we're removing all the export icons

const TripTitleExport: React.FC<any> = () => {
  // Return null (no UI)
  return null;
};

export default TripTitleExport; 